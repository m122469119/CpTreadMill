<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--分別對應上面左圆角的半径，上面右圆角的半径，下面左圆角的半径，下面右圆角的半径-->
    <!-- 填充颜色 -->
    <solid android:color="@color/stateview_button_select"/>

    <corners
        android:topLeftRadius="5dp"
        android:topRightRadius="5dp"
        android:bottomLeftRadius="5dp"
        android:bottomRightRadius="5dp"/>
    <!--设置渐变-->
    <!--<gradient android:startColor="#9cff00"
        android:endColor="#197600"
        android:angle="270"/>-->
    <!-- 线的宽度，颜色灰色 -->
    <stroke android:width="1dp" android:color="@color/stateview_button_select"/>
</shape>